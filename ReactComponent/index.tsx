import * as React from 'react';

import style from './index.scss';

export interface ReactComponentProps {
}

class ReactComponent extends React.Component<ReactComponentProps, any> {
  render() {
    return (
      <div>
        
      </div>
    );
  }
}

export default ReactComponent;